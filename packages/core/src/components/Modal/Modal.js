import ModalAnimator from './ModalAnimator';
import ModalWindow, { WindowTitle as ModalWindowTitle } from './Window';
import ModalWithCross from './WithCross';

const Modal = {
  ModalAnimator,
  ModalWindow,
  ModalWindowTitle,
  ModalWithCross,
};

export default Modal;
{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "babel-jest": "^23.0.1",
    "concurrently": "^3.5.1",
    "jest": "^23.1.0",
    "lerna": "^2.9.0"
  },
  "scripts": {
    "dev": "concurrently \"cd packages/core && yarn build:watch\" \"cd packages/styleguide && yarn start\" \"cd packages/common-styles && yarn build:watch\"",
    "test": "jest --projects packages/*"
  },
  "eslintConfig": {
    "extends": "./eslint.js"
  }
}
